# ─── Sensitive — NEVER commit ─────────────────────────────────
ssh-key-*.key
ssh-key-*.key.pub
*.pem
*.crt
.certs/
.env
.env.*
*credentials*.json
*secret*.json

# ─── PSQ templates (contain company quotation data) ──────────
1.*.xlsx
2.*.xlsx
*.xlsx
*.eml

# ─── AI models (multi-GB, regenerated locally / R2 sync) ─────
models/
UpscaleImages/
SampleImages/
NewUI/
"email test"/
files.zip
*.onnx
*.safetensors

# ─── Backup snapshots (per CLAUDE.md Rule 16 — strict retention) ─
backup*.html
mockup-*.html

# ─── Backend service source (deployed separately to Fly.io) ───
docker-compose.yml
pslink-collabora/
pslink-pdf-worker/
pslink-wopi/

# ─── Cloudflare Workers (wrangler local dev cache) ───────────
.wrangler/
.dev.vars

# ─── Node + IDE ──────────────────────────────────────────────
node_modules/
.chrome-dev/
.vscode/
.idea/
# .claude — ignore everything except memory-sync hook files
# (whitelisted entries below are tracked for seamless restore on new machine)
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/hooks/*
!.claude/hooks/sync-memory.ps1
.psup-convert/
*.log

# ─── Memory backup (auto-synced via Stop hook) ───────────────
# Gitignored to avoid history noise from per-session memory updates;
# OneDrive sync handles cloud backup of .memory_backup/
.memory_backup/

# ─── OS ──────────────────────────────────────────────────────
Thumbs.db
.DS_Store
desktop.ini
